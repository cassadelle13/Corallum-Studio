# React Flow Editor - Windmill Style

Современный редактор workflow с интерфейсом в стиле Windmill, интегрированный с LangGraph бекендом.

## Особенности

- ✨ Визуальный редактор workflow на базе React Flow
- 🎨 Дизайн в стиле Windmill UI
- 🇷🇺 Полная поддержка русского языка
- 🔌 Интеграция с LangGraph API
- 💾 Сохранение и экспорт workflow
- 🧩 Поддержка различных типов узлов (действия, ветвления, циклы)
- 📝 Редактор кода с поддержкой Python, TypeScript, Go, Bash, SQL

## Технологии

- **React 19** + **TypeScript**
- **Vite** - быстрая сборка
- **React Flow** - визуальный редактор графов
- **Zustand** - управление состоянием
- **Axios** - HTTP клиент
- **Lucide React** - иконки

## Быстрый старт

### 1. Установка зависимостей

```bash
pnpm install
```

### 2. Запуск dev сервера

```bash
pnpm dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### 3. Сборка для production

```bash
pnpm build
```

## Структура проекта

```
src/
├── components/
│   ├── Toolbar.tsx       # Панель инструментов
│   ├── Sidebar.tsx       # Боковая панель с компонентами
│   ├── FlowCanvas.tsx    # Холст для редактирования
│   └── NodeEditor.tsx    # Редактор узлов
├── store/
│   └── flowStore.ts      # Управление состоянием
├── App.tsx               # Главный компонент
└── App.css               # Стили
```

## Кастомизация

### Изменение цветовой схемы

Отредактируйте CSS переменные в `src/App.css`:

```css
:root {
  --primary-color: #3b82f6;
  --secondary-color: #6366f1;
  --success-color: #10b981;
  /* ... */
}
```

### Добавление новых типов узлов

Отредактируйте массив `nodeTypes` в `src/components/Sidebar.tsx`:

```typescript
const nodeTypes = [
  { 
    type: 'custom', 
    icon: YourIcon, 
    label: 'Ваш узел', 
    description: 'Описание' 
  },
  // ...
];
```

### Изменение текстов интерфейса

Все тексты находятся непосредственно в компонентах. Для полноценной локализации рекомендуется использовать `i18next`.

## Интеграция с бекендом

Приложение отправляет запросы к LangGraph API:

```typescript
// Endpoint для тестирования
POST http://localhost:8000/invoke

// Формат запроса
{
  "input": "текст",
  "thread_id": "уникальный_id"
}
```

Убедитесь, что бекенд запущен:

```bash
cd ..
python3 langgraph_app.py
```

## Команды

- `pnpm dev` - запуск dev сервера
- `pnpm build` - сборка для production
- `pnpm preview` - предпросмотр production сборки
- `pnpm lint` - проверка кода

## Дальнейшие улучшения

- [ ] Добавить i18n для мультиязычности
- [ ] Реализовать сохранение в базу данных
- [ ] Добавить авторизацию пользователей
- [ ] Расширить типы узлов
- [ ] Добавить валидацию workflow
- [ ] Реализовать версионирование
- [ ] Добавить темную тему

## Лицензия

MIT
